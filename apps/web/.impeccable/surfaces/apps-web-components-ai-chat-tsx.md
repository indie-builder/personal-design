---
version: 1
slug: "apps-web-components-ai-chat-tsx"
primary_target: "apps/web/components/ai-chat.tsx"
related_targets: ["apps/web/components/ai-chat.module.css","apps/web/components/ai-chat-ui.tsx"]
---

# AI 问答

Mode: Operate. Code-led refinement of the first-version mobile-first layout. Preserve Pi Harness/OpenUI, agents, history and context compression.

## Direction contract

THESIS: Honor the user's original mobile screenshot and first-version layout on both phone and desktop; polish its controls and typography without recomposing the experience. Remove the disclaimer entirely.

OWN-WORLD: Project-neutral backgrounds and text, Albert Sans with Chinese system fallback. All icons are neutral; pale blue only for selected states and focus. White/dark paper surfaces inherit the global theme. Original Menu and Plus buttons remain; agent name and avatar stay centered. Default name is 生成式 UI 助手; it now embeds the 轻舟协作 business case (intake → comparison → trial → chart review → decision) with natural-language starters and the full official OpenUI library. This case stays in the default business agent; the renderer follows docs/design/controls/openui-mobile.md and supports other tasks, including content-only answers without forms or actions; avatars are randomly assigned from the 15 male illustrations in the user-provided20-image archive and persisted; existing female selections migrate without losing agents or conversations.

STORY: Open the centered header dropdown list to select or create an agent; open Menu for a full-height drawer confined to the mobile frame. The shared header now always shows “← AI 问答” outside the mobile demo, aligned with the other product pages. The drawer has a static conversation title, followed only by new-chat and history; no appearance controls or storage-location footer. Type in the bottom composer, then refine streamed answers through text and generated controls. Creation still requires only name and system prompt. Its editor is an absolutely positioned child of the mobile chat container (inset:0, no browser-level dialog/top layer), occupies only that frame, and temporarily hides the mounted chat: back and centered title at top, independently scrollable fields, one full-width submit button fixed at bottom. Entry must not summon the keyboard, name Enter moves to prompt, back/Escape retains the session draft, successful creation clears it, and mobile VisualViewport resizing keeps the footer visible.

FIRST VIEWPORT: An actual mobile presentation: 480px maximum centered portrait frame on desktop, full-width on phones. The same 15px headings/13px body and mobile spacing apply at both window sizes. Shared product back-title belongs to the external site header, independently positioned on wide screens so the demo keeps its16px top inset; the mobile frame keeps Menu left, agent center, Plus right. Empty welcome and three quiet vertical business-case starters sit in the message area; clicking sends the request directly, and each touch-first choice contains a natural-language question plus one-line outcome; no arrow icon or keyboard shortcuts; the composer is docked at the bottom in empty and latest-message states, and hides while reading older messages. No suggestion chips beneath it. User messages align right, assistant content and generated groups left. Shell body/list13px at1.65, header/welcome/navigation15px on every device. Shell editable fields use13px/400 with13px placeholders; avatar32px and composer padding6px 8px 6px 12px. Generated answers follow [their scoped contract](ai-chat-answers.md): body13px, labels12px, captions12px, heading15px, metrics18px, inputs13px, placeholders13px and icons14px, inherited from the shell `.page --chat-*` scale, with unboxed groups,12px group spacing and full-width answer/portal/edit action grids. Single actions span a row; two short actions share equal columns only above320px content-container width, while narrower containers or three-plus actions stack full-width. Each group has at most one primary, with outlined secondary alternatives; text buttons use10px corners,13px text/20px line box,10px 12px padding and44px minimum height. Shell navigation, send and copy icons keep their existing shapes; all shell button icons inherit14px; all actual input values inherit body13px, including composer and creation forms. Empty and selected select text use13px in both focus states. OpenUI and creation fields use20px line boxes,8px 12px padding and44px minimum height; generated textareas start at80px. Existing group12px/Form16px spacing and chart geometry remain; browser-native zoom is not restricted.

FORM: User-pinned mobile screenshot and explicitly preferred first-version layout; no concept seed. Motion now explains mobile navigation: dropdown unfolds from its trigger (180/140ms), navigation slides within a stationary clipped frame (220/180ms), creation pushes in from right and returns right (240/180ms). Creation keeps the underlying chat mounted/inert, retains exiting DOM until commit, and can reverse from its current transform. New messages enter once, never per streamed chunk; send/stop and active-generation dots give small feedback. Keyboard/reduced-motion/background settle instantly. Single continuous blue focus contour belongs to the whole composer; no inner textarea rectangle or doubled outline. Preserve keyboard, reduced-motion and modal focus recovery.

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance

No generated rasters. No disclaimer or replacement warning beneath the composer.

CONSISTENCY: The shared size scope also reaches official body portals and chart tooltips. Creation labels use12px. Source inspection covers the82 official definitions; deterministic10-group replay and opened-overlay checks have separate evidence in docs/design/execution/ai-chat-consistency-audit.md. This is an AI-chat scope, not a site-wide audit.

SCROLL CHROME: The header keeps its directional behavior. The composer is hidden only while away from the latest-message region (80px tolerance), regardless of scroll reversal; return to latest restores it. It slides down/fades over180ms, stays mounted/inert while hidden, and never changes message viewport height. The measured bottom inset follows multiline input. The independent centered latest-message button remains reachable; drafts persist, keyboard and reduced motion use immediate transitions.
