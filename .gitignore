node_modules/
.next/
out/
dist/
*.tsbuildinfo

.DS_Store

# 旧项目残留文件的本地备份，不入库
.legacy-backup/

# 上游仓库下载缓存（sync 脚本临时目录）
packages/layout-compositions/.upstream/

# impeccable 技能运行产物（评审截图、构建状态机）；config/design.json 是系统记录，入库
.impeccable/review/
.impeccable/build/

# 同步脚本中断残留的下载临时文件（成功后改名为目标，残留即垃圾）
*.part

# Generated from the captured atlas runtime and the synchronized bilingual catalog.
apps/web/public/ai-coding-atlas/

# Server-only credentials
.env*
!.env.example
